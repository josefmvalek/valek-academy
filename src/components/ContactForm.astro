---
import { tinaField } from '@tinacms/astro/tina-field';

interface Props {
  contact?: {
    formBadge?: string;
    formBadgeNote?: string;
    formTitle?: string;
    formSubtitle?: string;
    submitButtonText?: string;
    securityNote?: string;
    web3formsKey?: string;
    phoneFormatted?: string;
    programLabel?: string;
    formatLabel?: string;
    formatInPersonTitle?: string;
    formatInPersonNote?: string;
    formatOnlineTitle?: string;
    formatOnlineNote?: string;
    formatUndecidedTitle?: string;
    formatUndecidedNote?: string;
    parentNameLabel?: string;
    childNameLabel?: string;
    phoneLabel?: string;
    emailLabel?: string;
    levelLabel?: string;
    levelLabelNote?: string;
    daysLabel?: string;
    daysNote?: string;
    daysTimeNote?: string;
    messageLabel?: string;
    stepsTitle?: string;
    step1Text?: string;
    step2Text?: string;
    step3Text?: string;
    adultNameLabel?: string;
    adultSubmitButtonText?: string;
    teenNameLabel?: string;
    teenChildNameLabel?: string;
    teenSubmitButtonText?: string;
    [key: string]: any;
  };
}

const { contact = {} } = Astro.props;

const formBadge = contact.formBadge || "🎁 1. LEKCE 100% ZDARMA";
const formBadgeNote = contact.formBadgeNote || "Nezávazná přihláška";
const formTitle = contact.formTitle || "Rezervace ukázkové lekce hrou";
const formSubtitle = contact.formSubtitle || "Vyplňte krátký formulář a pan Válek se vám ozve s návrhem volných dnů v doučovně v Uherském Hradišti.";
const submitButtonText = contact.submitButtonText || "🎁 Rezervovat 1. lekci ZDARMA";
const securityNote = contact.securityNote || "🔒 Vaše kontaktní údaje jsou v naprostém bezpečí. Odesláním berete na vědomí zpracování údajů pro domluvu lekce.";
const phoneFormatted = contact.phoneFormatted || "+420 792 372 642";
const web3formsKey = contact.web3formsKey || import.meta.env.PUBLIC_WEB3FORMS_KEY || "f49fa95f-e8a5-4b5b-b462-11980de8d1bc";

const programLabel = contact.programLabel || "O jaký program máte zájem?";
const parentNameLabel = contact.parentNameLabel || "Jméno rodiče";
const childNameLabel = contact.childNameLabel || "Jméno a věk dítěte";
const phoneLabel = contact.phoneLabel || "Telefonní číslo";
const emailLabel = contact.emailLabel || "E-mailová adresa";
const levelLabel = contact.levelLabel || "Třída / Současná úroveň";
const levelLabelNote = contact.levelLabelNote || "(pro výběr her a party vrstevníků)";
const daysLabel = contact.daysLabel || "Které dny se vám nejvíc hodí?";
const daysNote = contact.daysNote || "(lze vybrat i více dnů)";
const daysTimeNote = contact.daysTimeNote || "Čas 14:00–18:00 přizpůsobujeme skupince";
const messageLabel = contact.messageLabel || "Poznámka / S čím potřebujete pomoci?";
const stepsTitle = contact.stepsTitle || "Co se stane po odeslání?";
const step1Text = contact.step1Text || "Odešlete nezávaznou poptávku (1 min)";
const step2Text = contact.step2Text || "Pan Válek se vám do 24 h ozve s volnými časy";
const step3Text = contact.step3Text || "Dítě si přijde nezávazně zahrát 1. lekci zdarma";

const selectedGroupCalloutTitle = contact.selectedGroupCalloutTitle || "🎯 Vybraná zvířecí skupinka z rozvrhu";
const selectedGroupClearText = contact.selectedGroupClearText || "✕ Změnit skupinku";
const formSuccessTitle = contact.formSuccessTitle || "Děkuji za zprávu!";
const formSuccessText = contact.formSuccessText || "Požadavek jsem přijal a ozvu se vám co nejdříve s návrhem termínu ukázkové lekce zdarma v Uherském Hradišti.";
const formErrorTitle = contact.formErrorTitle || "Omlouvám se, odeslání se nezdařilo.";
const formErrorText = contact.formErrorText || `Můžete mi prosím zavolat na ${phoneFormatted} nebo napsat přímo na WhatsApp?`;

const adultNameLabel = contact.adultNameLabel || "Vaše celé jméno";
const adultSubmitButtonText = contact.adultSubmitButtonText || "📝 Zapsat se na čekací listinu pro dospělé";
const teenNameLabel = contact.teenNameLabel || "Jméno studenta / zájemce";
const teenChildNameLabel = contact.teenChildNameLabel || "Jméno rodiče";
const teenSubmitButtonText = contact.teenSubmitButtonText || "📝 Zapsat se na čekací listinu pro maturanty";

const receiptBadge = contact.receiptBadge || "✓ REZERVACE V POŘÁDKU PŘIJATA";
const receiptGuaranteeBadge = contact.receiptGuaranteeBadge || "🎁 1. lekce 100% ZDARMA";
const receiptGreetingTitle = contact.receiptGreetingTitle || "Děkuji za zprávu";
const receiptWhatToBring = contact.receiptWhatToBring || "Pouze přezůvky a dobrou náladu. Deskovky, karty, didaktické materiály i pití jsou zajištěny.";
const receiptLocation = contact.receiptLocation || "📍 Růžová 1238, Uherské Hradiště (naproti ZŠ UNESCO – 90 s pěšky)";
const receiptPrice = contact.receiptPrice || "100% ZDARMA (nezávazně vyzkoušíte)";
const receiptGoogleCalBtnText = contact.receiptGoogleCalBtnText || "Uložit do Google Kalendáře";
const receiptWhatsAppBtnText = contact.receiptWhatsAppBtnText || "Napsat panu Válkovi na WhatsApp";
const whatsappConfirmationMessage = contact.whatsappConfirmationMessage || "Dobrý den, pane Válku, právě jsem odeslal/a rezervaci lekce pro {attendee}{group}. Rád/a bych se domluvil/a na konkrétním termínu.";
const calendarEventTitle = contact.calendarEventTitle || "1. ukázková lekce angličtiny ZDARMA | VALEK ACADEMY";
const calendarEventDesc = contact.calendarEventDesc || "Ukázková lekce angličtiny hrou v malé skupince pro {attendee}{group}.\nLektor: Josef Válek (+420 792 372 642)\nWeb: https://valekacademy.cz\nS sebou: Pouze přezůvky a dobrou náladu!";
---

<div class="lg:col-span-7 w-full max-w-full min-w-0">
  <div class="bg-surface-card rounded-card-xl p-3 sm:p-8 md:p-10 border-2 border-amber-300/80 shadow-card relative w-full max-w-full min-w-0 overflow-hidden box-border">
    
    <div class="hidden lg:block mb-6">
      <div class="flex flex-wrap items-center gap-2 mb-2">
        <span class="px-2.5 py-1 rounded-full text-[11px] font-black uppercase tracking-wider bg-amber-400 text-espresso-950 shadow-2xs" data-tina-field={tinaField(contact, 'formBadge')} data-edit-key="contact.formBadge" data-edit-label="Formulář - štítek">
          {formBadge}
        </span>
        <span class="text-xs text-stone-500 font-bold" data-tina-field={tinaField(contact, 'formBadgeNote')} data-edit-key="contact.formBadgeNote" data-edit-label="Formulář - poznámka ke štítku">{formBadgeNote}</span>
      </div>
      <h3 class="font-heading font-black text-fluid-h2 text-espresso-950 mb-1 break-words" data-tina-field={tinaField(contact, 'formTitle')} data-edit-key="contact.formTitle" data-edit-label="Formulář - nadpis">
        {formTitle}
      </h3>
      <p class="text-xs sm:text-sm text-stone-600 font-normal" data-tina-field={tinaField(contact, 'formSubtitle')} data-edit-key="contact.formSubtitle" data-edit-label="Formulář - podnadpis">
        {formSubtitle}
      </p>
    </div>

    <form
      id="contact-form"
      class="space-y-4 sm:space-y-5"
      data-access-key={web3formsKey}
      data-adult-name-label={adultNameLabel}
      data-adult-btn-text={adultSubmitButtonText}
      data-teen-name-label={teenNameLabel}
      data-teen-child-label={teenChildNameLabel}
      data-teen-btn-text={teenSubmitButtonText}
      data-whatsapp-template={whatsappConfirmationMessage}
      data-cal-title={calendarEventTitle}
      data-cal-desc={calendarEventDesc}
      data-receipt-location={receiptLocation}
      data-contact-phone={(contact.phone || '420792372642').replace(/\D/g, '')}
      data-contact-location={contact.location || "Růžová 1238, Uherské Hradiště"}
    >
      <input type="checkbox" name="botcheck" class="hidden" style="display: none;" />
      <input type="hidden" id="selected-group-input" name="selected_group" value="" />

      <!-- Form interactive fields container (hidden after successful submission) -->
      <div id="form-interactive-fields" class="space-y-4 sm:space-y-5">
        <!-- Selected Animal Group Callout (Active when clicked from weekly schedule) -->
        <div id="selected-group-callout" class="hidden p-3.5 sm:p-4 rounded-card-lg bg-amber-100/90 border-2 border-amber-400 shadow-2xs transition-all w-full min-w-0">
        <div class="flex flex-wrap sm:flex-nowrap items-center justify-between gap-2.5 sm:gap-3">
          <div class="flex items-center gap-3 min-w-0 flex-1">
            <span id="callout-group-emoji" class="w-10 h-10 rounded-card-md bg-amber-300 text-espresso-950 flex items-center justify-center text-xl flex-shrink-0 font-bold shadow-2xs border border-amber-400">
              🦘
            </span>
            <div class="min-w-0 flex-1">
              <div class="text-[11px] font-black uppercase tracking-wider text-amber-900" data-tina-field={tinaField(contact, 'selectedGroupCalloutTitle')} data-edit-key="contact.selectedGroupCalloutTitle" data-edit-label="Zvolená skupinka nadpis">
                {selectedGroupCalloutTitle}
              </div>
              <div id="callout-group-title" class="font-heading font-black text-sm sm:text-base text-espresso-950 truncate">
                🦘 Kangaroos (Pondělí 12:30 – 13:30)
              </div>
              <div id="callout-group-meta" class="text-[11px] text-stone-600 font-medium">
                1.–3. třída • 1. ukázková lekce 100% ZDARMA
              </div>
            </div>
          </div>
          <button
            type="button"
            id="clear-selected-group-btn"
            class="text-xs text-espresso-800 hover:text-red-700 font-bold px-2.5 py-1.5 rounded-xl hover:bg-amber-200/80 transition-colors whitespace-nowrap cursor-pointer flex-shrink-0 border border-amber-300 bg-white/70"
            title="Zrušit a vybrat jiné zvířátko"
            data-tina-field={tinaField(contact, 'selectedGroupClearText')}
            data-edit-key="contact.selectedGroupClearText"
            data-edit-label="Zrušit výběr zvířátka"
          >
            {selectedGroupClearText}
          </button>
        </div>
      </div>

      <!-- Row 1: Segment Choice (Custom Rich Dropdown) -->
      <div class="relative custom-dropdown-container" id="custom-segment-dropdown">
        <label id="segment-label" class="block text-xs font-black uppercase tracking-wider text-espresso-950 mb-2">
          <span data-tina-field={tinaField(contact, 'programLabel')} data-edit-key="contact.programLabel" data-edit-label="Štítek výběru programu">{programLabel}</span> <span class="text-red-500">*</span>
        </label>

        <!-- Hidden native select for form submit, accessibility & Tina fallback -->
        <select
          id="segment-select"
          name="segment"
          required
          class="sr-only"
          tabindex="-1"
          aria-hidden="true"
        >
          <option value="Děti na ZŠ (Výuka hrou v malé skupince 4–8 dětí) – 1. LEKCE ZDARMA" selected>
            🎲 Děti na ZŠ (Skupinka 4–8 dětí hrou) – 1. LEKCE 100% ZDARMA [Otevřeno]
          </option>
          <option value="Středoškoláci & Maturita (90 min intenzivní bloky) – 1. LEKCE ZDARMA">
            🎓 Střední škola a maturita (90 min bloky) – 1. LEKCE 100% ZDARMA [Otevřeno]
          </option>
          <option value="Angličtina pro dospělé & Konverzace (Miniskupinky i individuál)">
            💼 Angličtina pro dospělé & Konverzace [Otevřeno / Dle domluvy]
          </option>
          <option value="SCIO testy & Přijímačky na SŠ (Čeština & Matematika)">
            📐 SCIO testy & Přijímačky na SŠ [Přednostní zápis]
          </option>
        </select>

        <!-- Custom Trigger Button -->
        <button
          type="button"
          id="segment-trigger"
          aria-haspopup="listbox"
          aria-expanded="false"
          aria-labelledby="segment-label segment-trigger-title"
          class="w-full flex items-center justify-between gap-2 sm:gap-3 p-3 sm:p-4 rounded-2xl bg-parchment-50 hover:bg-white border-2 border-amber-300/80 hover:border-amber-500 focus:outline-none focus:border-amber-500 focus:ring-4 focus:ring-amber-400/20 text-left transition-all cursor-pointer shadow-2xs group min-w-0"
        >
          <div class="flex items-center gap-2 sm:gap-3 min-w-0 flex-1">
            <div id="segment-trigger-icon" class="w-9 h-9 sm:w-11 sm:h-11 rounded-xl bg-gradient-to-br from-amber-300 to-amber-400 text-espresso-950 border border-amber-400/80 flex items-center justify-center text-lg sm:text-xl flex-shrink-0 group-hover:scale-105 transition-transform shadow-2xs font-bold">
              🎲
            </div>
            <div class="min-w-0 flex-1">
              <div class="flex items-center gap-1.5 sm:gap-2 flex-wrap">
                <span id="segment-trigger-title" class="font-heading font-black text-sm sm:text-base text-espresso-950 truncate max-w-full">
                  Děti na ZŠ (Skupinka 4–8 dětí hrou)
                </span>
                <span id="segment-trigger-badge" class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300 shadow-2xs">
                  🎁 1. lekce ZDARMA • Otevřeno
                </span>
              </div>
              <p id="segment-trigger-desc" class="text-[11px] sm:text-xs text-stone-600 font-normal truncate mt-0.5">
                Pro žáky 1.–9. třídy • Deskovky, aktivní mluvení a přirozený přízvuk
              </p>
            </div>
          </div>

          <div class="w-8 h-8 rounded-xl bg-parchment-200/80 group-hover:bg-amber-200/80 flex items-center justify-center flex-shrink-0 transition-all border border-parchment-300/80">
            <svg id="segment-chevron" class="dropdown-chevron w-4 h-4 text-espresso-950 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </div>
        </button>

        <!-- Custom Dropdown Menu Listbox -->
        <div
          id="segment-menu"
          role="listbox"
          tabindex="-1"
          class="custom-dropdown-menu absolute left-0 right-0 top-[calc(100%+8px)] z-50 bg-white/95 backdrop-blur-xl border-2 border-amber-400 rounded-2xl shadow-2xl p-2 space-y-1.5 transition-all duration-200 origin-top opacity-0 scale-95 pointer-events-none"
        >
          <!-- Option 1: Děti ZŠ -->
          <div
            role="option"
            aria-selected="true"
            data-value="Děti na ZŠ (Výuka hrou v malé skupince 4–8 dětí) – 1. LEKCE ZDARMA"
            data-icon="🎲"
            data-title="Děti na ZŠ (Skupinka 4–8 dětí hrou)"
            data-badge="🎁 1. lekce ZDARMA • Otevřeno"
            data-badge-class="bg-emerald-100 text-emerald-900 border border-emerald-300 shadow-2xs"
            data-desc="Pro žáky 1.–9. třídy • Deskovky, aktivní mluvení a přirozený přízvuk"
            class="segment-option p-3 rounded-xl border-2 border-amber-500 bg-amber-50/90 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt shadow-2xs"
          >
            <div class="flex items-center gap-3 min-w-0">
              <span class="w-10 h-10 rounded-xl bg-gradient-to-br from-amber-300 to-amber-400 text-espresso-950 flex items-center justify-center text-lg flex-shrink-0 group-hover/opt:scale-110 transition-transform shadow-2xs font-bold border border-amber-400">
                🎲
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                  <span class="font-heading font-black text-sm text-espresso-950">Děti na ZŠ (Skupinka 4–8 dětí hrou)</span>
                  <span class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300">
                    🎁 1. lekce ZDARMA • Otevřeno
                  </span>
                </div>
                <p class="text-[11px] text-stone-600 font-normal truncate mt-0.5">Pro žáky 1.–9. třídy • Deskovky, aktivní mluvení a přirozený přízvuk</p>
              </div>
            </div>
            <span class="option-check text-base text-amber-700 font-black flex-shrink-0">✓</span>
          </div>

          <!-- Option 2: Středoškoláci & Maturita -->
          <div
            role="option"
            aria-selected="false"
            data-value="Středoškoláci & Maturita (90 min intenzivní bloky) – 1. LEKCE ZDARMA"
            data-icon="🎓"
            data-title="Střední škola & Maturita"
            data-badge="🎁 1. lekce ZDARMA • Otevřeno"
            data-badge-class="bg-emerald-100 text-emerald-900 border border-emerald-300"
            data-desc="Příprava k maturitě (didaktické testy & ústní část), Cambridge zkoušky a konverzace"
            class="segment-option p-3 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-3 min-w-0">
              <span class="w-10 h-10 rounded-xl bg-amber-100 text-espresso-950 flex items-center justify-center text-lg flex-shrink-0 group-hover/opt:scale-110 transition-transform shadow-2xs border border-amber-300">
                🎓
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                  <span class="font-heading font-black text-sm text-espresso-950">Střední škola & Maturita (90 min)</span>
                  <span class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300">
                    🎁 1. lekce ZDARMA • Otevřeno
                  </span>
                </div>
                <p class="text-[11px] text-stone-500 font-normal truncate mt-0.5">Příprava k maturitě (didaktické testy & ústní část), plynulost mluvení bez trémy</p>
              </div>
            </div>
            <span class="option-check text-base text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 3: Dospělí -->
          <div
            role="option"
            aria-selected="false"
            data-value="Angličtina pro dospělé & Konverzace (Miniskupinky i individuál)"
            data-icon="💼"
            data-title="Angličtina pro dospělé & Konverzace"
            data-badge="💬 Otevřeno • Domluva"
            data-badge-class="bg-amber-100 text-amber-900 border border-amber-300"
            data-desc="Mluvení bez stresu a ostychu, praktická slovní zásoba do práce i na cesty"
            class="segment-option p-3 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-3 min-w-0">
              <span class="w-10 h-10 rounded-xl bg-parchment-200 text-espresso-950 flex items-center justify-center text-lg flex-shrink-0 group-hover/opt:scale-110 transition-transform shadow-2xs border border-parchment-300">
                💼
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                  <span class="font-heading font-black text-sm text-espresso-950">Angličtina pro dospělé & Konverzace</span>
                  <span class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">
                    💬 Otevřeno • Domluva
                  </span>
                </div>
                <p class="text-[11px] text-stone-500 font-normal truncate mt-0.5">Mluvení bez stresu a ostychu, praktická slovní zásoba do práce i na cesty</p>
              </div>
            </div>
            <span class="option-check text-base text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 4: SCIO testy -->
          <div
            role="option"
            aria-selected="false"
            data-value="SCIO testy & Přijímačky na SŠ (Čeština & Matematika)"
            data-icon="📐"
            data-title="SCIO testy & Přijímačky na SŠ"
            data-badge="🎯 Přednostní zápis"
            data-badge-class="bg-parchment-200 text-stone-800 border border-parchment-300"
            data-desc="Cílená příprava pro 5. a 9. třídy na jednotné přijímací zkoušky na gymnázia a SŠ"
            class="segment-option p-3 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-3 min-w-0">
              <span class="w-10 h-10 rounded-xl bg-parchment-200 text-espresso-950 flex items-center justify-center text-lg flex-shrink-0 group-hover/opt:scale-110 transition-transform shadow-2xs border border-parchment-300">
                📐
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-2 flex-wrap">
                  <span class="font-heading font-black text-sm text-espresso-950">SCIO testy & Přijímačky na SŠ</span>
                  <span class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">
                    🎯 Přednostní zápis
                  </span>
                </div>
                <p class="text-[11px] text-stone-500 font-normal truncate mt-0.5">Cílená příprava pro 5. a 9. třídy na jednotné přijímací zkoušky na gymnázia a SŠ</p>
              </div>
            </div>
            <span class="option-check text-base text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>
        </div>
      </div>

      <!-- Místo výuky: 100% osobně v UH -->
      <input type="hidden" name="forma" value="Osobně v doučovně (Uherské Hradiště, Růžová 1238)" />
      <div class="p-3 sm:p-4 rounded-2xl bg-amber-50/90 border-2 border-amber-300 shadow-2xs flex flex-wrap sm:flex-nowrap items-center justify-between gap-2.5 sm:gap-3 text-xs min-w-0">
        <div class="flex items-center gap-2.5 text-espresso-950 font-bold min-w-0 flex-1">
          <span class="w-8 h-8 sm:w-10 sm:h-10 rounded-xl bg-amber-200 text-espresso-950 flex items-center justify-center text-base sm:text-lg flex-shrink-0 border border-amber-300">🏡</span>
          <div class="min-w-0 flex-1">
            <span class="block text-[10px] font-black uppercase tracking-wider text-amber-900">Místo výuky</span>
            <span class="text-xs sm:text-sm text-espresso-950 font-bold break-words">Doučovna Růžová 1238, Uherské Hradiště (naproti ZŠ UNESCO)</span>
          </div>
        </div>
        <span class="px-2.5 py-1 rounded-full text-[10px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300 shadow-2xs flex-shrink-0">
          100% naživo
        </span>
      </div>

      <!-- Row 2: Parent Name and Child Name -->
      <div class="grid sm:grid-cols-2 gap-4 transition-all duration-300" id="name-fields-container">
        <div id="parent-name-col" class="transition-all duration-300">
          <label for="name" id="name-label" class="flex items-baseline justify-between gap-1 text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5 min-h-[18px]">
            <span>
              <span id="name-label-text" data-tina-field={tinaField(contact, 'parentNameLabel')} data-edit-key="contact.parentNameLabel" data-edit-label="Štítek jména rodiče">{parentNameLabel}</span> <span class="text-red-500">*</span>
            </span>
          </label>
          <input
            type="text"
            id="name"
            name="name"
            required
            autocomplete="name"
            placeholder="např. Alena Nováková"
            class="w-full px-4 py-3 rounded-xl bg-parchment-50 border border-parchment-300 text-espresso-950 placeholder-stone-400 focus:outline-none focus:border-amber-500 focus:bg-white transition-colors text-base sm:text-sm font-medium"
          />
        </div>

        <div id="child-name-col" class="transition-all duration-300">
          <label for="child-name" id="child-name-label" class="flex items-baseline justify-between gap-1 text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5 min-h-[18px]">
            <span id="child-name-label-text" data-tina-field={tinaField(contact, 'childNameLabel')} data-edit-key="contact.childNameLabel" data-edit-label="Štítek jména dítěte">{childNameLabel}</span>
            <span id="child-name-hint" class="text-stone-400 text-[10px] font-normal normal-case tracking-normal">(a věk)</span>
          </label>
          <input
            type="text"
            id="child-name"
            name="child_name"
            autocomplete="off"
            placeholder="např. Tomáš, 10 let (4. třída)"
            class="w-full px-4 py-3 rounded-xl bg-parchment-50 border border-parchment-300 text-espresso-950 placeholder-stone-400 focus:outline-none focus:border-amber-500 focus:bg-white transition-colors text-base sm:text-sm font-medium"
          />
        </div>
      </div>

      <!-- Row 3: Phone & Email with Smart Formatting and Micro-interactions -->
      <div class="grid sm:grid-cols-2 gap-4">
        <div>
          <label for="phone" class="block text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5">
            <span data-tina-field={tinaField(contact, 'phoneLabel')} data-edit-key="contact.phoneLabel" data-edit-label="Štítek telefonu">{phoneLabel}</span> <span class="text-red-500">*</span>
          </label>
          <div class="relative">
            <input
              type="tel"
              id="phone"
              name="phone"
              required
              autocomplete="tel"
              aria-describedby="phone-feedback"
              placeholder="+420 777 000 000"
              class="w-full px-4 py-3 pr-10 rounded-xl bg-parchment-50 border border-parchment-300 text-espresso-950 placeholder-stone-400 focus:outline-none focus:border-amber-500 focus:bg-white focus:ring-2 focus:ring-amber-400/20 transition-all text-base sm:text-sm font-medium"
            />
            <span id="phone-valid-icon" class="absolute right-3.5 top-3 text-base text-emerald-600 font-black hidden">✓</span>
          </div>
          <div id="phone-feedback" class="text-[11px] mt-1.5 min-h-[16px] transition-all"></div>
        </div>

        <div>
          <label for="email" class="block text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5">
            <span data-tina-field={tinaField(contact, 'emailLabel')} data-edit-key="contact.emailLabel" data-edit-label="Štítek e-mailu">{emailLabel}</span> <span class="text-red-500">*</span>
          </label>
          <div class="relative">
            <input
              type="email"
              id="email"
              name="email"
              required
              autocomplete="email"
              aria-describedby="email-feedback"
              placeholder="alena.novakova@email.cz"
              class="w-full px-4 py-3 pr-10 rounded-xl bg-parchment-50 border border-parchment-300 text-espresso-950 placeholder-stone-400 focus:outline-none focus:border-amber-500 focus:bg-white focus:ring-2 focus:ring-amber-400/20 transition-all text-base sm:text-sm font-medium"
            />
            <span id="email-valid-icon" class="absolute right-3.5 top-3 text-base text-emerald-600 font-black hidden">✓</span>
          </div>
          <div id="email-feedback" class="text-[11px] mt-1.5 min-h-[16px] transition-all"></div>
        </div>
      </div>

      <!-- Group-Dependent Preference Fields (Auto-collapsed when group is selected from schedule) -->
      <div id="schedule-preference-fields" class="space-y-4 sm:space-y-5 transition-all">
        <!-- Row 4: Grade Level (Custom Rich Dropdown) -->
        <div class="relative custom-dropdown-container" id="custom-level-dropdown">
        <label id="level-label" class="block text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5">
          <span id="level-label-title" data-tina-field={tinaField(contact, 'levelLabel')} data-edit-key="contact.levelLabel" data-edit-label="Štítek úrovně">{levelLabel}</span> <span id="level-label-note" class="text-stone-400 text-[10px] font-normal" data-tina-field={tinaField(contact, 'levelLabelNote')} data-edit-key="contact.levelLabelNote" data-edit-label="Poznámka k úrovni">{levelLabelNote}</span>
        </label>

        <!-- Hidden native select for form submit, accessibility & fallback -->
        <select
          id="level"
          name="level"
          class="sr-only"
          tabindex="-1"
          aria-hidden="true"
        >
          <option value="ZŠ 1. stupeň (1.–3. třída)">ZŠ 1. stupeň (1.–3. třída) – Malí objevitelé & deskovky</option>
          <option value="ZŠ 2. stupeň (4.–6. třída)" selected>ZŠ 2. stupeň (4.–6. třída) – Dobrodruzi & Karak</option>
          <option value="ZŠ 2. stupeň (7.–9. třída)">ZŠ 2. stupeň (7.–9. třída) – Mladí stratégové & Příprava na SŠ</option>
          <option value="Střední škola / Maturita">Střední škola / Maturita – 90 min bloky [Otevřeno]</option>
          <option value="Dospělý začátečník / Konverzace">Dospělý / Konverzace [Otevřeno / Domluva]</option>
          <option value="SCIO přijímačky">SCIO přijímačky 5. / 9. třída [Přednostní zápis]</option>
        </select>

        <!-- Custom Trigger Button for Level -->
        <button
          type="button"
          id="level-trigger"
          aria-haspopup="listbox"
          aria-expanded="false"
          aria-labelledby="level-label level-trigger-title"
          class="w-full flex items-center justify-between gap-2 sm:gap-3 p-3 sm:p-3.5 rounded-2xl bg-parchment-50 hover:bg-white border-2 border-parchment-300 hover:border-amber-400 focus:outline-none focus:border-amber-500 focus:ring-4 focus:ring-amber-400/20 text-left transition-all cursor-pointer shadow-2xs group min-w-0"
        >
          <div class="flex items-center gap-2.5 sm:gap-3 min-w-0 flex-1">
            <div id="level-trigger-icon" class="w-10 h-10 rounded-xl bg-amber-100 text-espresso-950 border border-amber-300 flex items-center justify-center text-lg flex-shrink-0 group-hover:scale-105 transition-transform shadow-2xs font-bold">
              🐉
            </div>
            <div class="min-w-0 flex-1">
              <div class="flex items-center gap-1.5 sm:gap-2 flex-wrap">
                <span id="level-trigger-title" class="font-heading font-black text-sm text-espresso-950 truncate max-w-full">
                  ZŠ 2. stupeň (4.–6. třída)
                </span>
                <span id="level-trigger-badge" class="px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300 shadow-2xs">
                  ⭐ Nejpopulárnější • Karak
                </span>
              </div>
              <p id="level-trigger-desc" class="text-[11px] text-stone-500 font-normal truncate mt-0.5">
                Dobrodruzi & Karak • Deskovky, rozmluvení a sebevědomí do školy
              </p>
            </div>
          </div>

          <div class="w-8 h-8 rounded-lg bg-parchment-200/70 group-hover:bg-amber-100 flex items-center justify-center flex-shrink-0 transition-all border border-parchment-300/80">
            <svg id="level-chevron" class="dropdown-chevron w-4 h-4 text-espresso-950 transition-transform duration-200" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
            </svg>
          </div>
        </button>

        <!-- Custom Dropdown Menu Listbox for Level -->
        <div
          id="level-menu"
          role="listbox"
          tabindex="-1"
          class="custom-dropdown-menu absolute left-0 right-0 top-[calc(100%+6px)] z-50 bg-white/95 backdrop-blur-xl border-2 border-amber-400 rounded-2xl shadow-2xl p-2 space-y-1 transition-all duration-200 origin-top opacity-0 scale-95 pointer-events-none max-h-80 overflow-y-auto"
        >
          <!-- Option 1: 1.-3. trida -->
          <div
            role="option"
            aria-selected="false"
            data-value="ZŠ 1. stupeň (1.–3. třída)"
            data-icon="🎒"
            data-title="ZŠ 1. stupeň (1.–3. třída)"
            data-badge="🎲 Malí objevitelé"
            data-badge-class="bg-amber-100 text-amber-900 border border-amber-300"
            data-desc="Hravý Karak Junior, Dobble, obrázky a čistá rodilá výslovnost od mala"
            class="level-option p-2.5 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-100 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform">
                🎒
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">ZŠ 1. stupeň (1.–3. třída)</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-bold uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">Malí objevitelé</span>
                </div>
                <p class="text-[10px] text-stone-500 font-normal truncate">Karak Junior, Dobble, pexesa a výslovnost od mala</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 2: 4.-6. trida (selected default) -->
          <div
            role="option"
            aria-selected="true"
            data-value="ZŠ 2. stupeň (4.–6. třída)"
            data-icon="🐉"
            data-title="ZŠ 2. stupeň (4.–6. třída)"
            data-badge="⭐ Nejpopulárnější • Karak"
            data-badge-class="bg-emerald-100 text-emerald-900 border border-emerald-300"
            data-desc="Dobrodruzi & Karak • Deskovky, rozmluvení a sebevědomí do školy"
            class="level-option p-2.5 rounded-xl border-2 border-amber-500 bg-amber-50/90 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt shadow-2xs"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-200 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform font-bold">
                🐉
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">ZŠ 2. stupeň (4.–6. třída)</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-black uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300">⭐ Nejpopulárnější • Karak</span>
                </div>
                <p class="text-[10px] text-stone-600 font-normal truncate">Dobrodruzi & Karak • Deskovky, rozmluvení a sebevědomí do školy</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0">✓</span>
          </div>

          <!-- Option 3: 7.-9. trida -->
          <div
            role="option"
            aria-selected="false"
            data-value="ZŠ 2. stupeň (7.–9. třída)"
            data-icon="⚔️"
            data-title="ZŠ 2. stupeň (7.–9. třída)"
            data-badge="🏆 Mladí stratégové"
            data-badge-class="bg-amber-100 text-amber-900 border border-amber-300"
            data-desc="Mladí stratégové, Codenames, přirozený přízvuk a příprava na SŠ"
            class="level-option p-2.5 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-100 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform">
                ⚔️
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">ZŠ 2. stupeň (7.–9. třída)</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-bold uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">Mladí stratégové</span>
                </div>
                <p class="text-[10px] text-stone-500 font-normal truncate">Codenames, plynulá konverzace a příprava na přijímačky</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 4: SS -->
          <div
            role="option"
            aria-selected="false"
            data-value="Střední škola / Maturita"
            data-icon="🎓"
            data-title="Střední škola / Maturita"
            data-badge="🎁 1. lekce ZDARMA • Otevřeno"
            data-badge-class="bg-emerald-100 text-emerald-900 border border-emerald-300"
            data-desc="Příprava na ústní maturitu i didaktické testy, Cambridge certifikáty"
            class="level-option p-2.5 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-100 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform">
                🎓
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">Střední škola / Maturita</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-bold uppercase tracking-wider bg-emerald-100 text-emerald-900 border border-emerald-300">Otevřeno (90 min)</span>
                </div>
                <p class="text-[10px] text-stone-500 font-normal truncate">Ústní maturita, didaktické testy a plynulost mluvení</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 5: Dospělí -->
          <div
            role="option"
            aria-selected="false"
            data-value="Dospělý začátečník / Konverzace"
            data-icon="☕"
            data-title="Dospělý / Konverzace bez stresu"
            data-badge="💬 Otevřeno • Domluva"
            data-badge-class="bg-amber-100 text-amber-900 border border-amber-300"
            data-desc="Rozmluvení bez strachu z chyb, praktická angličtina na cesty a do práce"
            class="level-option p-2.5 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-200 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform">
                ☕
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">Dospělý / Konverzace</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-bold uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">Otevřeno</span>
                </div>
                <p class="text-[10px] text-stone-500 font-normal truncate">Mluvení bez stresu, slovní zásoba na cesty a do práce</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>

          <!-- Option 6: SCIO -->
          <div
            role="option"
            aria-selected="false"
            data-value="SCIO přijímačky"
            data-icon="📐"
            data-title="SCIO testy & Přijímačky"
            data-badge="🎯 Přednostní zápis"
            data-badge-class="bg-amber-100 text-amber-900 border border-amber-300"
            data-desc="Příprava pro 5. a 9. třídy na jednotné přijímací zkoušky na gymnázia"
            class="level-option p-2.5 rounded-xl border-2 border-transparent hover:border-amber-300 hover:bg-parchment-50 cursor-pointer transition-all flex items-center justify-between gap-3 group/opt"
          >
            <div class="flex items-center gap-2.5 min-w-0">
              <span class="w-8 h-8 rounded-lg bg-amber-200 text-espresso-950 flex items-center justify-center text-base flex-shrink-0 group-hover/opt:scale-110 transition-transform">
                📐
              </span>
              <div class="min-w-0">
                <div class="flex items-center gap-1.5 flex-wrap">
                  <span class="font-heading font-black text-xs sm:text-sm text-espresso-950">SCIO testy & Přijímačky</span>
                  <span class="px-1.5 py-0.5 rounded-full text-[9px] font-bold uppercase tracking-wider bg-amber-100 text-amber-900 border border-amber-300">Přednostní zápis</span>
                </div>
                <p class="text-[10px] text-stone-500 font-normal truncate">Strategie řešení testů Cermat a Scio na gymnázia</p>
              </div>
            </div>
            <span class="option-check text-sm text-amber-700 font-black flex-shrink-0 hidden">✓</span>
          </div>
        </div>
      </div>

      <!-- Row 4b: Preferred Days Multi-Select -->
      <div>
        <div class="flex flex-wrap items-baseline justify-between gap-1 mb-2">
          <label class="block text-xs font-bold uppercase tracking-wider text-espresso-950">
            <span id="days-label-title" data-tina-field={tinaField(contact, 'daysLabel')} data-edit-key="contact.daysLabel" data-edit-label="Preferované dny">{daysLabel}</span> <span id="days-note-text" class="text-amber-800 text-[11px] font-semibold lowercase" data-tina-field={tinaField(contact, 'daysNote')} data-edit-key="contact.daysNote" data-edit-label="Poznámka ke dnům">{daysNote}</span>
          </label>
          <span id="days-time-note" class="text-[11px] text-stone-500 font-medium" data-tina-field={tinaField(contact, 'daysTimeNote')} data-edit-key="contact.daysTimeNote" data-edit-label="Časový tip">{daysTimeNote}</span>
        </div>

        <!-- Interactive Days Selector -->
        <input type="hidden" id="preferred-days-input" name="preferred_days" value="Kdykoliv po telefonické domluvě" />
        <div class="grid grid-cols-3 sm:grid-cols-6 gap-1.5 sm:gap-2 w-full max-w-full min-w-0" id="days-buttons-grid">
          <button
            type="button"
            data-day="Pondělí"
            aria-pressed="false"
            class="day-pill py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-parchment-300 bg-parchment-50 text-xs font-bold text-espresso-950 text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">Po</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-stone-500 font-normal truncate max-w-full block pointer-events-none">Pondělí</span>
          </button>
          <button
            type="button"
            data-day="Úterý"
            aria-pressed="false"
            class="day-pill py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-parchment-300 bg-parchment-50 text-xs font-bold text-espresso-950 text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">Út</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-stone-500 font-normal truncate max-w-full block pointer-events-none">Úterý</span>
          </button>
          <button
            type="button"
            data-day="Středa"
            aria-pressed="false"
            class="day-pill py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-parchment-300 bg-parchment-50 text-xs font-bold text-espresso-950 text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">St</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-stone-500 font-normal truncate max-w-full block pointer-events-none">Středa</span>
          </button>
          <button
            type="button"
            data-day="Čtvrtek"
            aria-pressed="false"
            class="day-pill py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-parchment-300 bg-parchment-50 text-xs font-bold text-espresso-950 text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">Čt</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-stone-500 font-normal truncate max-w-full block pointer-events-none">Čtvrtek</span>
          </button>
          <button
            type="button"
            data-day="Pátek"
            aria-pressed="false"
            class="day-pill py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-parchment-300 bg-parchment-50 text-xs font-bold text-espresso-950 text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">Pá</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-stone-500 font-normal truncate max-w-full block pointer-events-none">Pátek</span>
          </button>
          <button
            type="button"
            data-day="Kdykoliv"
            aria-pressed="true"
            class="day-pill active py-2 px-1 sm:py-2.5 sm:px-2 rounded-xl border-2 border-amber-500 bg-amber-400 text-espresso-950 shadow-xs text-xs font-bold text-center cursor-pointer flex flex-col items-center justify-center gap-0.5 min-w-0 w-full touch-manipulation select-none active:scale-95 transition-[border-color,background-color,transform] duration-150"
          >
            <span class="text-xs sm:text-sm font-black pointer-events-none">✨ Vše</span>
            <span class="day-subtext text-[9px] sm:text-[10px] text-espresso-900 font-bold truncate max-w-full block pointer-events-none">Kdykoliv</span>
          </button>
        </div>
      </div>
      </div>

      <!-- Toggle to expand preferences if group is selected from schedule -->
      <div id="toggle-preferences-wrapper" class="hidden text-center py-1">
        <button
          type="button"
          id="toggle-preferences-btn"
          class="text-xs text-amber-900/80 hover:text-amber-950 font-bold underline hover:no-underline cursor-pointer inline-flex items-center gap-1.5 p-2 rounded-xl hover:bg-amber-100/60 transition-colors"
        >
          <span>⚙️ Upřesnit další volné dny nebo změnit ročník</span>
          <span id="toggle-preferences-icon">▾</span>
        </button>
      </div>

      <!-- Row 5: Message -->
      <div>
        <label for="message" class="block text-xs font-bold uppercase tracking-wider text-espresso-950 mb-1.5" data-tina-field={tinaField(contact, 'messageLabel')} data-edit-key="contact.messageLabel" data-edit-label="Štítek zprávy">
          {messageLabel}
        </label>
        <textarea
          id="message"
          name="message"
          rows="2"
          placeholder="Dobrý den, potřebovali bychom pomoci s mluvením a přípravou do školy..."
          class="w-full px-4 py-3 rounded-xl bg-parchment-50 border border-parchment-300 text-espresso-950 placeholder-stone-400 focus:outline-none focus:border-amber-500 focus:bg-white transition-colors text-base sm:text-sm resize-none font-medium"
        ></textarea>
      </div>

      <!-- Reassuring 3-Step Micro-Timeline -->
      <div class="p-3 sm:p-3.5 rounded-2xl bg-parchment-100/90 border border-parchment-300/80 space-y-1.5 text-espresso-950">
        <div class="text-[10px] font-black uppercase tracking-wider text-amber-900 flex items-center gap-1.5">
          <span>⏱️</span> <span data-tina-field={tinaField(contact, 'stepsTitle')} data-edit-key="contact.stepsTitle" data-edit-label="Co se stane">{stepsTitle}</span>
        </div>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-2 text-xs">
          <div class="flex items-start gap-1.5 min-w-0">
            <span class="w-4 h-4 rounded-full bg-amber-300 text-espresso-950 flex items-center justify-center font-black text-[10px] flex-shrink-0 mt-0.5">1</span>
            <span class="text-stone-700 text-[11px] leading-tight break-words min-w-0" data-tina-field={tinaField(contact, 'step1Text')} data-edit-key="contact.step1Text" data-edit-label="Krok 1">{step1Text}</span>
          </div>
          <div class="flex items-start gap-1.5 min-w-0">
            <span class="w-4 h-4 rounded-full bg-amber-300 text-espresso-950 flex items-center justify-center font-black text-[10px] flex-shrink-0 mt-0.5">2</span>
            <span class="text-stone-700 text-[11px] leading-tight break-words min-w-0" data-tina-field={tinaField(contact, 'step2Text')} data-edit-key="contact.step2Text" data-edit-label="Krok 2">{step2Text}</span>
          </div>
          <div class="flex items-start gap-1.5 min-w-0">
            <span class="w-4 h-4 rounded-full bg-emerald-500 text-white flex items-center justify-center font-black text-[10px] flex-shrink-0 mt-0.5">3</span>
            <span class="text-stone-700 text-[11px] leading-tight break-words min-w-0" data-tina-field={tinaField(contact, 'step3Text')} data-edit-key="contact.step3Text" data-edit-label="Krok 3">{step3Text}</span>
          </div>
        </div>
      </div>

      <!-- Submit Button with Gold Pulse and 3D Haptic Shadow -->
      <button
        type="submit"
        id="submit-button"
        class="btn-gold-pulse shadow-dice-btn active:shadow-dice-btn-active w-full inline-flex items-center justify-center gap-2 sm:gap-3 py-3.5 sm:py-5 px-4 sm:px-8 rounded-card-md font-heading font-black text-sm sm:text-lg text-espresso-950 bg-gradient-to-b from-amber-300 via-amber-400 to-amber-500 border border-amber-600 disabled:opacity-50 cursor-pointer transition-transform min-w-0"
      >
        <span id="button-text" class="truncate min-w-0" data-tina-field={tinaField(contact, 'submitButtonText')} data-edit-key="contact.submitButtonText" data-edit-label="Text tlačítka">{submitButtonText}</span>
        <svg id="button-icon" class="w-5 h-5 text-espresso-950 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M14 5l7 7m0 0l-7 7m7-7H3" />
        </svg>
      </button>

      <!-- Status Alerts -->
      <div id="form-success" class="hidden p-4 rounded-xl bg-emerald-50 border border-emerald-300 text-emerald-900 text-sm flex items-center gap-3">
        <span class="text-2xl">✅</span>
        <div>
          <strong data-tina-field={tinaField(contact, 'formSuccessTitle')} data-edit-key="contact.formSuccessTitle" data-edit-label="Úspěch: nadpis">{formSuccessTitle}</strong> <span data-tina-field={tinaField(contact, 'formSuccessText')} data-edit-key="contact.formSuccessText" data-edit-label="Úspěch: text">{formSuccessText}</span>
        </div>
      </div>

      <div id="form-error" class="hidden p-4 rounded-xl bg-rose-50 border border-rose-300 text-rose-900 text-sm flex items-center gap-3">
        <span class="text-2xl">⚠️</span>
        <div>
          <strong data-tina-field={tinaField(contact, 'formErrorTitle')} data-edit-key="contact.formErrorTitle" data-edit-label="Chyba: nadpis">{formErrorTitle}</strong> <span data-tina-field={tinaField(contact, 'formErrorText')} data-edit-key="contact.formErrorText" data-edit-label="Chyba: text">{formErrorText}</span>
        </div>
      </div>

      <p class="text-[11px] text-stone-500 text-center font-medium leading-relaxed" data-tina-field={tinaField(contact, 'securityNote')} data-edit-key="contact.securityNote" data-edit-label="Bezpečnostní poznámka">
        <span>{securityNote}</span>
        <a href="/ochrana-osobnich-udaju" class="underline hover:text-amber-800 transition-colors ml-1 font-semibold" target="_blank" rel="noopener">Více informací v Zásadách ochrany osobních údajů & cookies.</a>
      </p>
      </div><!-- /#form-interactive-fields -->

      <!-- Booking Confirmation Receipt (Rich On-page Confirmation Card) -->
      <div id="booking-confirmation-receipt" class="hidden space-y-5">
        <div class="p-5 sm:p-7 rounded-card-lg bg-gradient-to-br from-emerald-50 via-white to-amber-50/50 border-2 border-emerald-500 shadow-md space-y-4">
          
          <div class="flex items-center gap-2 flex-wrap">
            <span class="px-3 py-1 rounded-full text-[11px] font-black uppercase tracking-wider bg-emerald-600 text-white shadow-2xs" data-tina-field={tinaField(contact, 'receiptBadge')} data-edit-key="contact.receiptBadge" data-edit-label="Potvrzení - odznak">
              {receiptBadge}
            </span>
            <span class="px-2.5 py-1 rounded-full text-[11px] font-black uppercase tracking-wider bg-amber-200 text-amber-950 border border-amber-300" data-tina-field={tinaField(contact, 'receiptGuaranteeBadge')} data-edit-key="contact.receiptGuaranteeBadge" data-edit-label="Potvrzení - odznak záruky">
              {receiptGuaranteeBadge}
            </span>
          </div>

          <div>
            <h4 class="font-heading font-black text-fluid-h3 text-espresso-950">
              <span data-tina-field={tinaField(contact, 'receiptGreetingTitle')} data-edit-key="contact.receiptGreetingTitle" data-edit-label="Potvrzení - nadpis">{receiptGreetingTitle}</span>, <span id="confirm-parent-name" class="text-amber-800 font-extrabold"></span>!
            </h4>
            <p class="text-xs sm:text-sm text-stone-700 mt-1.5 leading-relaxed font-normal">
              Rezervaci ukázkové lekce pro <strong id="confirm-child-name" class="text-espresso-950">vaše dítě</strong> jsem úspěšně zaznamenal. Do 24 hodin se vám ozvu na telefon <strong id="confirm-phone-num" class="text-espresso-950"></strong> nebo e-mailem s potvrzením volného místa v partě.
            </p>
          </div>

          <!-- Structured Ticket Card -->
          <div class="p-4 rounded-card-md bg-white border-2 border-amber-300 shadow-2xs space-y-3 text-xs sm:text-sm">
            <div class="flex items-center justify-between pb-2 border-b border-parchment-200 gap-2">
              <span class="text-stone-500 font-semibold">Vybraný program:</span>
              <span id="confirm-program-label" class="font-bold text-espresso-950 text-right">Děti na ZŠ (výuka hrou)</span>
            </div>
            <div id="confirm-group-row" class="flex items-center justify-between pb-2 border-b border-parchment-200 gap-2">
              <span class="text-stone-500 font-semibold">Zvířecí skupinka:</span>
              <span id="confirm-group-label" class="font-black text-amber-900 text-right">Dle domluvy</span>
            </div>
            <div class="flex items-center justify-between pb-2 border-b border-parchment-200 gap-2">
              <span class="text-stone-500 font-semibold">Preferované dny:</span>
              <span id="confirm-days-label" class="font-bold text-espresso-950 text-right">Kdykoliv v týdnu</span>
            </div>
            <div class="flex items-start justify-between pb-2 border-b border-parchment-200 gap-2">
              <span class="text-stone-500 font-semibold flex-shrink-0">Místo doučovny:</span>
              <span class="font-bold text-espresso-950 text-right" data-tina-field={tinaField(contact, 'receiptLocation')} data-edit-key="contact.receiptLocation" data-edit-label="Potvrzení - adresa">{receiptLocation}</span>
            </div>
            <div class="flex items-start justify-between pb-2 border-b border-parchment-200 gap-2">
              <span class="text-stone-500 font-semibold flex-shrink-0">Co si vzít s sebou:</span>
              <span class="font-medium text-stone-700 text-right" data-tina-field={tinaField(contact, 'receiptWhatToBring')} data-edit-key="contact.receiptWhatToBring" data-edit-label="Potvrzení - co s sebou">{receiptWhatToBring}</span>
            </div>
            <div class="flex items-center justify-between">
              <span class="text-stone-500 font-semibold">Cena:</span>
              <span class="font-black text-emerald-700 text-right text-sm sm:text-base" data-tina-field={tinaField(contact, 'receiptPrice')} data-edit-key="contact.receiptPrice" data-edit-label="Potvrzení - cena">{receiptPrice}</span>
            </div>
          </div>

          <!-- Calendar & WhatsApp Quick Action Bar -->
          <div class="pt-2 space-y-2.5">
            <div class="grid sm:grid-cols-2 gap-2.5">
              <a
                id="add-google-cal-btn"
                href="#"
                target="_blank"
                rel="noopener noreferrer"
                class="py-3 px-4 rounded-xl bg-white hover:bg-amber-50 text-espresso-950 border-2 border-amber-400 font-heading font-black text-xs sm:text-sm flex items-center justify-center gap-2 shadow-2xs transition-all hover:scale-[1.02] cursor-pointer"
              >
                <span>📅</span>
                <span data-tina-field={tinaField(contact, 'receiptGoogleCalBtnText')} data-edit-key="contact.receiptGoogleCalBtnText" data-edit-label="Potvrzení - tlačítko Kalendář">{receiptGoogleCalBtnText}</span>
              </a>

              <a
                id="confirm-whatsapp-btn"
                href="https://wa.me/420792372642"
                target="_blank"
                rel="noopener noreferrer"
                class="py-3 px-4 rounded-xl bg-emerald-600 hover:bg-emerald-500 text-white font-heading font-black text-xs sm:text-sm flex items-center justify-center gap-2 shadow-2xs transition-all hover:scale-[1.02] cursor-pointer"
              >
                <span>💬</span>
                <span data-tina-field={tinaField(contact, 'receiptWhatsAppBtnText')} data-edit-key="contact.receiptWhatsAppBtnText" data-edit-label="Potvrzení - tlačítko WhatsApp">{receiptWhatsAppBtnText}</span>
              </a>
            </div>

            <button
              type="button"
              id="reset-form-btn"
              class="w-full py-2.5 px-4 text-xs font-bold text-stone-500 hover:text-espresso-950 hover:underline transition-colors text-center cursor-pointer"
            >
              ↺ Chcete odeslat další rezervaci nebo přihlásit sourozence?
            </button>
          </div>

        </div>
      </div>

    </form>

  </div>
</div>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement | null;
  const submitBtn = document.getElementById('submit-button') as HTMLButtonElement | null;
  const btnText = document.getElementById('button-text');
  const successBox = document.getElementById('form-success');
  const errorBox = document.getElementById('form-error');
  const segmentSelect = document.getElementById('segment-select') as HTMLSelectElement | null;
  const defaultSubmitText = btnText?.textContent || "🎁 Rezervovat 1. lekci ZDARMA";

  // Dynamic elements for audience segment adaptation
  const parentNameCol = document.getElementById('parent-name-col');
  const nameLabelText = document.getElementById('name-label-text');
  const nameInput = document.getElementById('name') as HTMLInputElement | null;
  const childNameCol = document.getElementById('child-name-col');
  const childNameLabelText = document.getElementById('child-name-label-text');
  const childNameHint = document.getElementById('child-name-hint');
  const childNameInput = document.getElementById('child-name') as HTMLInputElement | null;

  const levelSelect = document.getElementById('level') as HTMLSelectElement | null;
  const levelLabelTitle = document.getElementById('level-label-title');
  const levelLabelNote = document.getElementById('level-label-note');
  const daysTimeNote = document.getElementById('days-time-note');
  const messageInput = document.getElementById('message') as HTMLTextAreaElement | null;

  // Initial defaults
  const defaultParentLabel = nameLabelText?.textContent?.trim() || "Jméno rodiče";
  const defaultChildLabel = childNameLabelText?.textContent?.trim() || "Jméno a věk dítěte";
  const defaultLevelLabel = levelLabelTitle?.textContent?.trim() || "Třída / současná úroveň";
  const defaultLevelNote = levelLabelNote?.textContent?.trim() || "(pro výběr her a party vrstevníků)";
  const defaultDaysTimeNote = daysTimeNote?.textContent?.trim() || "Čas 14:00–18:00 přizpůsobujeme skupince";
  const defaultMessagePlaceholder = messageInput?.placeholder || "Dobrý den, potřebovali bychom pomoci s mluvením a přípravou do školy...";

  function applySegmentMode(segmentVal: string) {
    if (!nameLabelText || !nameInput) return;

    const formEl = document.getElementById('contact-form') as HTMLFormElement | null;
    const adultNameLabelVal = formEl?.dataset.adultNameLabel || "Vaše celé jméno";
    const adultBtnTextVal = formEl?.dataset.adultBtnText || "📝 Zapsat se na čekací listinu pro dospělé";
    const teenNameLabelVal = formEl?.dataset.teenNameLabel || "Jméno studenta / zájemce";
    const teenChildLabelVal = formEl?.dataset.teenChildLabel || "Jméno rodiče";
    const teenBtnTextVal = formEl?.dataset.teenBtnText || "📝 Zapsat se na čekací listinu pro maturanty";

    const lower = (segmentVal || '').toLowerCase();
    const isAdult = lower.includes('dospěl') || lower.includes('konverzace');
    const isTeen = lower.includes('středoškoláci') || lower.includes('maturita') || lower.includes('střední');
    const isScio = lower.includes('scio') || lower.includes('přijímačky');

    const customLevelDropdown = document.getElementById('custom-level-dropdown');

    if (isAdult) {
      // --- ADULT CONVERSATION MODE ---
      customLevelDropdown?.classList.add('hidden');
      parentNameCol?.classList.add('sm:col-span-2');
      nameLabelText.textContent = adultNameLabelVal;
      nameInput.placeholder = "např. Ing. Petr Dvořák";

      childNameCol?.classList.add('hidden');
      if (childNameInput) {
        childNameInput.required = false;
        childNameInput.value = '';
      }

      // Auto-select Adult level
      if (levelSelect && !levelSelect.value.toLowerCase().includes('dospěl') && !levelSelect.value.toLowerCase().includes('konverzace')) {
        const adultOpt = Array.from(levelSelect.options).find(o => o.value.toLowerCase().includes('dospěl') || o.value.toLowerCase().includes('konverzace'));
        if (adultOpt) {
          levelSelect.value = adultOpt.value;
          levelSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      if (daysTimeNote) daysTimeNote.textContent = "Odpolední a večerní termíny (17:00–20:00) dle domluvy";
      if (messageInput) messageInput.placeholder = "Dobrý den, mám zájem o konverzaci bez stresu / potřebuji angličtinu do práce v oboru...";
      if (btnText) btnText.textContent = adultBtnTextVal;

    } else if (isTeen) {
      // --- TEEN / HIGH SCHOOL / MATURITA MODE ---
      customLevelDropdown?.classList.add('hidden');
      parentNameCol?.classList.remove('sm:col-span-2');
      nameLabelText.textContent = teenNameLabelVal;
      nameInput.placeholder = "např. Tereza Dvořáková (3. ročník Gymnázia)";

      childNameCol?.classList.remove('hidden');
      if (childNameLabelText) childNameLabelText.textContent = teenChildLabelVal;
      if (childNameHint) childNameHint.textContent = "(nepovinné)";
      if (childNameInput) {
        childNameInput.placeholder = "např. Jan Dvořák (otec)";
        childNameInput.required = false;
      }

      // Auto-select High School level
      if (levelSelect && !levelSelect.value.toLowerCase().includes('střední') && !levelSelect.value.toLowerCase().includes('maturita')) {
        const ssOpt = Array.from(levelSelect.options).find(o => o.value.toLowerCase().includes('střední') || o.value.toLowerCase().includes('maturita'));
        if (ssOpt) {
          levelSelect.value = ssOpt.value;
          levelSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      if (daysTimeNote) daysTimeNote.textContent = "Odpolední 90min bloky přizpůsobujeme rozvrhu SŠ";
      if (messageInput) messageInput.placeholder = "Dobrý den, potřebuji se připravit k maturitě / zbavit se nervozity při mluvení...";
      if (btnText) btnText.textContent = teenBtnTextVal;

    } else if (isScio) {
      // --- SCIO PREPARATION MODE ---
      customLevelDropdown?.classList.add('hidden');
      parentNameCol?.classList.remove('sm:col-span-2');
      nameLabelText.textContent = defaultParentLabel;
      nameInput.placeholder = "např. Alena Nováková";

      childNameCol?.classList.remove('hidden');
      if (childNameLabelText) childNameLabelText.textContent = "Jméno žáka";
      if (childNameHint) childNameHint.textContent = "(5. nebo 9. třída)";
      if (childNameInput) {
        childNameInput.placeholder = "např. Tomáš, 9. třída (příprava na gymnázium)";
        childNameInput.required = false;
      }

      if (levelSelect) {
        const scioOpt = Array.from(levelSelect.options).find(o => o.value.toLowerCase().includes('scio'));
        if (scioOpt) {
          levelSelect.value = scioOpt.value;
          levelSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      if (daysTimeNote) daysTimeNote.textContent = "Přípravné bloky přizpůsobujeme rozvrhu";
      if (messageInput) messageInput.placeholder = "Dobrý den, máme zájem o přípravu na přijímací zkoušky na víceleté gymnázium / SŠ...";
      if (btnText) btnText.textContent = "📝 Zapsat se na přednostní čekací listinu";

    } else {
      // --- KIDS MODE (DEFAULT) ---
      customLevelDropdown?.classList.remove('hidden');
      parentNameCol?.classList.remove('sm:col-span-2');
      nameLabelText.textContent = defaultParentLabel;
      nameInput.placeholder = "např. Alena Nováková";

      childNameCol?.classList.remove('hidden');
      if (childNameLabelText) childNameLabelText.textContent = defaultChildLabel;
      if (childNameHint) childNameHint.textContent = "(a věk)";
      if (childNameInput) {
        childNameInput.placeholder = "např. Tomáš, 10 let (4. třída)";
        childNameInput.required = false;
      }

      if (levelLabelTitle) levelLabelTitle.textContent = defaultLevelLabel;
      if (levelLabelNote) levelLabelNote.textContent = defaultLevelNote;

      if (levelSelect && (levelSelect.value.toLowerCase().includes('dospěl') || levelSelect.value.toLowerCase().includes('střední') || levelSelect.value.toLowerCase().includes('scio'))) {
        const kidsOpt = Array.from(levelSelect.options).find(o => o.value.includes('4.–6.') || o.value.includes('1.–3.'));
        if (kidsOpt) {
          levelSelect.value = kidsOpt.value;
          levelSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      if (daysTimeNote) daysTimeNote.textContent = defaultDaysTimeNote;
      if (messageInput) messageInput.placeholder = defaultMessagePlaceholder;
      if (btnText) btnText.textContent = defaultSubmitText;
    }
  }

  function checkUrlSegmentParam() {
    try {
      const urlParams = new URLSearchParams(window.location.search);
      const programParam = urlParams.get('program') || '';
      const hash = window.location.hash.toLowerCase();

      let targetSegment = '';
      if (programParam === 'dospeli' || programParam === 'adults' || hash.includes('dospeli') || hash.includes('adults')) {
        targetSegment = 'adults';
      } else if (programParam === 'maturita' || programParam === 'stredoskoly' || programParam === 'teens' || hash.includes('maturita') || hash.includes('stredoskoly')) {
        targetSegment = 'teens';
      } else if (programParam === 'scio' || hash.includes('scio')) {
        targetSegment = 'scio';
      }

      if (targetSegment && segmentSelect) {
        let matchingValue = '';
        if (targetSegment === 'adults') {
          matchingValue = Array.from(segmentSelect.options).find(o => o.value.toLowerCase().includes('dospěl') || o.value.toLowerCase().includes('konverzace'))?.value || '';
        } else if (targetSegment === 'teens') {
          matchingValue = Array.from(segmentSelect.options).find(o => o.value.toLowerCase().includes('středoškoláci') || o.value.toLowerCase().includes('maturita') || o.value.toLowerCase().includes('střední'))?.value || '';
        } else if (targetSegment === 'scio') {
          matchingValue = Array.from(segmentSelect.options).find(o => o.value.toLowerCase().includes('scio'))?.value || '';
        }

        if (matchingValue) {
          segmentSelect.value = matchingValue;
          segmentSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }

      // Pre-fill message from calculator if coming from /cenik or external query params
      const weeksParam = urlParams.get('weeks');
      const freqParam = urlParams.get('freq');
      const buddyParam = urlParams.get('buddy');
      if (weeksParam || freqParam || buddyParam) {
        const msgEl = document.getElementById('message') as HTMLTextAreaElement | null;
        if (msgEl && (!msgEl.value || msgEl.value.trim() === '')) {
          const modeLabel = targetSegment === 'teens' ? "2. stupeň, SŠ & dospělí (90minutové lekce)" : "Děti ZŠ (60minutové lekce)";
          const buddyLine = buddyParam === '1' ? "\n• Přijdeme ve dvou (kamarád ze třídy / sourozenec) – chceme uplatnit slevu 5 % pro obě děti." : "";
          const weeksStr = weeksParam ? `${weeksParam} týdnů` : "pololetí";
          const freqStr = freqParam ? `${freqParam}× týdně` : "1× týdně";
          msgEl.value = `Dobrý den, pane Válku, máme zájem o balíček z kalkulátoru (${modeLabel}):\n• Období: ${weeksStr}\n• Frekvence: ${freqStr}${buddyLine}\n\nRádi bychom nejprve nezávazně vyzkoušeli 1. lekci 100% zdarma.`;
        }
      }
    } catch {}
  }

  segmentSelect?.addEventListener('change', () => {
    if (segmentSelect) applySegmentMode(segmentSelect.value);
  });

  // Initial check on load & page transitions
  checkUrlSegmentParam();
  document.addEventListener('DOMContentLoaded', checkUrlSegmentParam);
  document.addEventListener('astro:page-load', checkUrlSegmentParam);
  if (segmentSelect) {
    applySegmentMode(segmentSelect.value);
  }

  // --- Custom Rich Dropdowns Controller ---
  function setupCustomDropdown(config: {
    containerId: string;
    triggerId: string;
    menuId: string;
    chevronId: string;
    selectId: string;
    iconId: string;
    titleId: string;
    badgeId?: string;
    descId?: string;
    optionClass: string;
  }) {
    const container = document.getElementById(config.containerId);
    const trigger = document.getElementById(config.triggerId) as HTMLButtonElement | null;
    const menu = document.getElementById(config.menuId);
    const chevron = document.getElementById(config.chevronId);
    const select = document.getElementById(config.selectId) as HTMLSelectElement | null;
    const iconEl = document.getElementById(config.iconId);
    const titleEl = document.getElementById(config.titleId);
    const badgeEl = config.badgeId ? document.getElementById(config.badgeId) : null;
    const descEl = config.descId ? document.getElementById(config.descId) : null;

    if (!trigger || !menu || !select) return;
    const triggerBtn: HTMLButtonElement = trigger;
    const menuEl: HTMLElement = menu;
    const selectEl: HTMLSelectElement = select;
    const options: HTMLElement[] = container ? Array.from(container.querySelectorAll<HTMLElement>(`.${config.optionClass}`)) : [];

    let isOpen = false;

    function openMenu() {
      // Close other custom dropdowns first
      document.querySelectorAll<HTMLElement>('.custom-dropdown-menu.dropdown-open').forEach((m: HTMLElement) => {
        if (m !== menuEl) {
          m.classList.remove('dropdown-open', 'opacity-100', 'scale-100', 'pointer-events-auto');
          m.classList.add('opacity-0', 'scale-95', 'pointer-events-none');
          const p = m.closest('.custom-dropdown-container');
          p?.querySelector('[aria-haspopup="listbox"]')?.setAttribute('aria-expanded', 'false');
          p?.querySelector('.dropdown-chevron')?.classList.remove('rotate-180');
        }
      });

      isOpen = true;
      triggerBtn.setAttribute('aria-expanded', 'true');
      menuEl.classList.add('dropdown-open', 'opacity-100', 'scale-100', 'pointer-events-auto');
      menuEl.classList.remove('opacity-0', 'scale-95', 'pointer-events-none');
      if (chevron) chevron.classList.add('rotate-180');
    }

    function closeMenu() {
      isOpen = false;
      triggerBtn.setAttribute('aria-expanded', 'false');
      menuEl.classList.remove('dropdown-open', 'opacity-100', 'scale-100', 'pointer-events-auto');
      menuEl.classList.add('opacity-0', 'scale-95', 'pointer-events-none');
      if (chevron) chevron.classList.remove('rotate-180');
    }

    triggerBtn.addEventListener('click', (e: MouseEvent) => {
      e.stopPropagation();
      if (isOpen) {
        closeMenu();
      } else {
        openMenu();
      }
    }, true);

    function selectOption(opt: HTMLElement) {
      const val = opt.dataset.value || '';
      const icon = opt.dataset.icon || '';
      const title = opt.dataset.title || '';
      const badge = opt.dataset.badge || '';
      const badgeClass = opt.dataset.badgeClass || '';
      const desc = opt.dataset.desc || '';

      selectEl.value = val;
      selectEl.dispatchEvent(new Event('change', { bubbles: true }));

      if (iconEl) iconEl.textContent = icon;
      if (titleEl) titleEl.textContent = title;
      if (badgeEl) {
        if (badge) {
          badgeEl.textContent = badge;
          badgeEl.className = `px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider ${badgeClass}`;
          badgeEl.classList.remove('hidden');
        } else {
          badgeEl.classList.add('hidden');
        }
      }
      if (descEl) descEl.textContent = desc;

      options.forEach((o: HTMLElement) => {
        const isMatch = o.dataset.value === val;
        o.setAttribute('aria-selected', isMatch ? 'true' : 'false');
        const check = o.querySelector('.option-check');
        if (isMatch) {
          o.classList.add('border-amber-500', 'bg-amber-50/90', 'shadow-2xs');
          o.classList.remove('border-transparent');
          check?.classList.remove('hidden');
        } else {
          o.classList.remove('border-amber-500', 'bg-amber-50/90', 'shadow-2xs');
          o.classList.add('border-transparent');
          check?.classList.add('hidden');
        }
      });

      closeMenu();
      triggerBtn.focus();
    }

    options.forEach((opt: HTMLElement) => {
      opt.addEventListener('click', (e: MouseEvent) => {
        e.stopPropagation();
        selectOption(opt);
      }, true);
    });

    // Sync custom dropdown when select value is changed externally (e.g. from GroupMatcher or Schedule)
    selectEl.addEventListener('change', () => {
      const currentVal = selectEl.value;
      const matchedOpt = Array.from(options).find(o => o.dataset.value === currentVal);
      if (matchedOpt) {
        const icon = matchedOpt.dataset.icon || '';
        const title = matchedOpt.dataset.title || '';
        const badge = matchedOpt.dataset.badge || '';
        const badgeClass = matchedOpt.dataset.badgeClass || '';
        const desc = matchedOpt.dataset.desc || '';

        if (iconEl) iconEl.textContent = icon;
        if (titleEl) titleEl.textContent = title;
        if (badgeEl) {
          if (badge) {
            badgeEl.textContent = badge;
            badgeEl.className = `px-2 py-0.5 rounded-full text-[10px] font-black uppercase tracking-wider ${badgeClass}`;
            badgeEl.classList.remove('hidden');
          } else {
            badgeEl.classList.add('hidden');
          }
        }
        if (descEl) descEl.textContent = desc;

        options.forEach((o: HTMLElement) => {
          const isMatch = o === matchedOpt;
          o.setAttribute('aria-selected', isMatch ? 'true' : 'false');
          const check = o.querySelector('.option-check');
          if (isMatch) {
            o.classList.add('border-amber-500', 'bg-amber-50/90', 'shadow-2xs');
            o.classList.remove('border-transparent');
            check?.classList.remove('hidden');
          } else {
            o.classList.remove('border-amber-500', 'bg-amber-50/90', 'shadow-2xs');
            o.classList.add('border-transparent');
            check?.classList.add('hidden');
          }
        });
      }
    });

    document.addEventListener('click', (e: MouseEvent) => {
      if (isOpen && !container?.contains(e.target as Node)) {
        closeMenu();
      }
    });

    document.addEventListener('keydown', (e: KeyboardEvent) => {
      if (isOpen && e.key === 'Escape') {
        closeMenu();
        triggerBtn.focus();
      }
    });
  }

  // Initialize Segment Dropdown
  setupCustomDropdown({
    containerId: 'custom-segment-dropdown',
    triggerId: 'segment-trigger',
    menuId: 'segment-menu',
    chevronId: 'segment-chevron',
    selectId: 'segment-select',
    iconId: 'segment-trigger-icon',
    titleId: 'segment-trigger-title',
    badgeId: 'segment-trigger-badge',
    descId: 'segment-trigger-desc',
    optionClass: 'segment-option'
  });

  // Initialize Level Dropdown
  setupCustomDropdown({
    containerId: 'custom-level-dropdown',
    triggerId: 'level-trigger',
    menuId: 'level-menu',
    chevronId: 'level-chevron',
    selectId: 'level',
    iconId: 'level-trigger-icon',
    titleId: 'level-trigger-title',
    badgeId: 'level-trigger-badge',
    descId: 'level-trigger-desc',
    optionClass: 'level-option'
  });

  // Smart Phone Formatting & Validation
  const phoneInput = document.getElementById('phone') as HTMLInputElement | null;
  const phoneFeedback = document.getElementById('phone-feedback');
  const phoneValidIcon = document.getElementById('phone-valid-icon');
  const emailInput = document.getElementById('email') as HTMLInputElement | null;
  const emailFeedback = document.getElementById('email-feedback');
  const emailValidIcon = document.getElementById('email-valid-icon');

  function formatPhoneCzech(val: string): string {
    let raw = val.trim();
    if (!raw) return '';

    // Handle Czech (+420) and Slovak (+421) prefixes
    let prefix = '+420 ';
    let digits = '';

    if (raw.startsWith('+421')) {
      prefix = '+421 ';
      digits = raw.slice(4).replace(/\D/g, '');
    } else if (raw.startsWith('+420')) {
      prefix = '+420 ';
      digits = raw.slice(4).replace(/\D/g, '');
    } else if (raw.startsWith('420') && raw.length > 9) {
      prefix = '+420 ';
      digits = raw.slice(3).replace(/\D/g, '');
    } else if (raw.startsWith('421') && raw.length > 9) {
      prefix = '+421 ';
      digits = raw.slice(3).replace(/\D/g, '');
    } else if (raw.startsWith('+')) {
      return raw; // Keep custom international format
    } else {
      prefix = '+420 ';
      digits = raw.replace(/\D/g, '');
    }

    // Limit to 9 digits for CZ/SK local numbers
    digits = digits.slice(0, 9);
    if (!digits) return '';

    const parts = digits.match(/.{1,3}/g) || [];
    return prefix + parts.join(' ');
  }

  phoneInput?.addEventListener('input', (e) => {
    if (!phoneInput) return;
    const isDelete = (e as any).inputType?.includes('delete');
    const curVal = phoneInput.value;

    // If user is deleting and field has only the prefix left, clear completely
    if (isDelete && (curVal === '+420 ' || curVal === '+420' || curVal === '+421 ' || curVal === '+421' || curVal === '+')) {
      phoneInput.value = '';
      if (phoneFeedback) phoneFeedback.innerHTML = '';
      phoneValidIcon?.classList.add('hidden');
      phoneInput.classList.remove('border-emerald-500', 'bg-emerald-50/20', 'border-amber-400');
      phoneInput.classList.add('border-parchment-300');
      return;
    }

    if (!isDelete && curVal.length > 0) {
      const formatted = formatPhoneCzech(curVal);
      if (formatted) {
        phoneInput.value = formatted;
      }
    }

    const digitsOnly = phoneInput.value.replace(/\D/g, '').replace(/^(420|421)/, '');
    if (digitsOnly.length === 9) {
      if (phoneFeedback) {
        phoneFeedback.innerHTML = '<span class="text-emerald-700 font-bold flex items-center gap-1"><span>✓</span> <span>Telefonní číslo je v pořádku</span></span>';
      }
      phoneValidIcon?.classList.remove('hidden');
      phoneInput.classList.remove('border-parchment-300', 'border-amber-400');
      phoneInput.classList.add('border-emerald-500', 'bg-emerald-50/20');
    } else if (digitsOnly.length > 0) {
      const remaining = 9 - digitsOnly.length;
      const plural = remaining === 1 ? 'číslici' : (remaining < 5 ? 'číslice' : 'číslic');
      if (phoneFeedback) {
        phoneFeedback.innerHTML = `<span class="text-stone-500">Zbývá zadat ještě ${remaining} ${plural}</span>`;
      }
      phoneValidIcon?.classList.add('hidden');
      phoneInput.classList.remove('border-emerald-500', 'bg-emerald-50/20', 'border-parchment-300');
      phoneInput.classList.add('border-amber-400');
    } else {
      if (phoneFeedback) phoneFeedback.innerHTML = '';
      phoneValidIcon?.classList.add('hidden');
      phoneInput.classList.remove('border-emerald-500', 'bg-emerald-50/20', 'border-amber-400');
      phoneInput.classList.add('border-parchment-300');
    }
  });

  phoneInput?.addEventListener('blur', () => {
    if (!phoneInput) return;
    if (phoneInput.value.trim().length > 0) {
      phoneInput.value = formatPhoneCzech(phoneInput.value);
    }
  });

  // Real-time Email validation
  emailInput?.addEventListener('input', () => {
    if (!emailInput) return;
    const val = emailInput.value.trim();
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;

    if (emailRegex.test(val)) {
      if (emailFeedback) {
        emailFeedback.innerHTML = '<span class="text-emerald-700 font-bold flex items-center gap-1"><span>✓</span> <span>E-mail je v pořádku</span></span>';
      }
      emailValidIcon?.classList.remove('hidden');
      emailInput.classList.remove('border-parchment-300', 'border-amber-400');
      emailInput.classList.add('border-emerald-500', 'bg-emerald-50/20');
    } else if (val.length > 0) {
      if (emailFeedback) {
        emailFeedback.innerHTML = '<span class="text-stone-500">Zadejte prosím platnou adresu (např. jmeno@email.cz)</span>';
      }
      emailValidIcon?.classList.add('hidden');
      emailInput.classList.remove('border-emerald-500', 'bg-emerald-50/20');
      emailInput.classList.add('border-amber-400');
    } else {
      if (emailFeedback) emailFeedback.innerHTML = '';
      emailValidIcon?.classList.add('hidden');
      emailInput.classList.remove('border-emerald-500', 'bg-emerald-50/20', 'border-amber-400');
      emailInput.classList.add('border-parchment-300');
    }
  });

  // Forma vyuky interactive pills
  const formaLabels = document.querySelectorAll<HTMLLabelElement>('.forma-option');
  formaLabels.forEach((lbl) => {
    const radio = lbl.querySelector<HTMLInputElement>('input[type="radio"]');
    radio?.addEventListener('change', () => {
      formaLabels.forEach((l) => {
        l.classList.remove('border-amber-500', 'bg-amber-50', 'shadow-2xs');
        l.classList.add('border-parchment-300', 'bg-parchment-50');
      });
      if (radio?.checked) {
        lbl.classList.remove('border-parchment-300', 'bg-parchment-50');
        lbl.classList.add('border-amber-500', 'bg-amber-50', 'shadow-2xs');
      }
    });
  });

  // Preferovane dny v tydnu interactive multi-select pills
  const selectedDays = new Set<string>(['Kdykoliv']);

  function updateDaysState() {
    const daysHiddenInput = document.getElementById('preferred-days-input') as HTMLInputElement | null;
    const isAll = selectedDays.size === 0 || selectedDays.has('Kdykoliv');
    
    if (daysHiddenInput) {
      if (isAll) {
        daysHiddenInput.value = 'Kdykoliv po telefonické domluvě';
      } else {
        const order = ['Pondělí', 'Úterý', 'Středa', 'Čtvrtek', 'Pátek'];
        const sorted = order.filter((d) => selectedDays.has(d));
        daysHiddenInput.value = sorted.join(', ');
      }
    }

    const currentPills = document.querySelectorAll<HTMLButtonElement>('.day-pill');
    currentPills.forEach((p) => {
      const d = p.dataset.day;
      const isSelected = isAll ? (d === 'Kdykoliv') : (!!d && selectedDays.has(d));
      const subText = p.querySelector<HTMLElement>('.day-subtext');

      p.setAttribute('aria-pressed', isSelected ? 'true' : 'false');
      
      if (isSelected) {
        p.classList.add('active', 'border-amber-500', 'bg-amber-400', 'shadow-xs');
        p.classList.remove('border-parchment-300', 'bg-parchment-50');
        if (subText) {
          subText.classList.add('text-espresso-900', 'font-bold');
          subText.classList.remove('text-stone-500', 'font-normal');
        }
      } else {
        p.classList.remove('active', 'border-amber-500', 'bg-amber-400', 'shadow-xs');
        p.classList.add('border-parchment-300', 'bg-parchment-50');
        if (subText) {
          subText.classList.remove('text-espresso-900', 'font-bold');
          subText.classList.add('text-stone-500', 'font-normal');
        }
      }
    });
  }

  function handleDaySelection(btn: HTMLButtonElement) {
    const day = btn.dataset.day;
    if (!day) return;

    if (day === 'Kdykoliv') {
      selectedDays.clear();
      selectedDays.add('Kdykoliv');
    } else {
      if (selectedDays.has('Kdykoliv')) {
        selectedDays.delete('Kdykoliv');
      }
      if (selectedDays.has(day)) {
        selectedDays.delete(day);
        if (selectedDays.size === 0) {
          selectedDays.add('Kdykoliv');
        }
      } else {
        selectedDays.add(day);
      }
    }
    updateDaysState();
  }

  let lastDayHandledTime = 0;

  function handleDayButtonInteraction(e: Event) {
    const target = e.target as HTMLElement | null;
    const btn = target?.closest<HTMLButtonElement>('.day-pill');
    if (!btn) return;

    e.stopPropagation();
    handleDaySelection(btn);
  }

  // Pointerdown captures mobile tap immediately (0ms delay) before TinaCMS or touch scrolling can swallow it
  document.addEventListener('pointerdown', (e) => {
    const target = e.target as HTMLElement | null;
    if (target?.closest('.day-pill')) {
      lastDayHandledTime = Date.now();
      handleDayButtonInteraction(e);
    }
  }, true);

  // Click listener for keyboard accessibility and fallback
  document.addEventListener('click', (e) => {
    const target = e.target as HTMLElement | null;
    if (target?.closest('.day-pill')) {
      if (Date.now() - lastDayHandledTime < 350) {
        e.preventDefault();
        e.stopPropagation();
        return;
      }
      handleDayButtonInteraction(e);
    }
  }, true);

  function syncDaysFromDOM() {
    const daysHiddenInput = document.getElementById('preferred-days-input') as HTMLInputElement | null;
    if (daysHiddenInput && daysHiddenInput.value && daysHiddenInput.value !== 'Kdykoliv po telefonické domluvě') {
      selectedDays.clear();
      daysHiddenInput.value.split(',').forEach((d) => {
        const trimmed = d.trim();
        if (trimmed) selectedDays.add(trimmed);
      });
      if (selectedDays.size === 0) selectedDays.add('Kdykoliv');
    }
    updateDaysState();
  }

  document.addEventListener('astro:page-load', syncDaysFromDOM);

  // --- Selected Animal Group Integration from ScheduleBlocks ---
  const calloutEl = document.getElementById('selected-group-callout');
  const calloutEmoji = document.getElementById('callout-group-emoji');
  const calloutTitle = document.getElementById('callout-group-title');
  const calloutMeta = document.getElementById('callout-group-meta');
  const selectedGroupInput = document.getElementById('selected-group-input') as HTMLInputElement | null;
  const clearGroupBtn = document.getElementById('clear-selected-group-btn');
  const segmentDropdown = document.getElementById('custom-segment-dropdown');
  const prefFields = document.getElementById('schedule-preference-fields');
  const togglePrefWrapper = document.getElementById('toggle-preferences-wrapper');
  const togglePrefBtn = document.getElementById('toggle-preferences-btn');
  const togglePrefIcon = document.getElementById('toggle-preferences-icon');

  function setSelectedGroup(detail: {
    id?: string;
    name?: string;
    day?: string;
    time?: string;
    cohort?: string;
    animal?: string;
    emoji?: string;
    status?: string;
  }) {
    if (!detail) return;

    if (selectedGroupInput) {
      selectedGroupInput.value = `${detail.emoji || ''} ${detail.animal || detail.name || ''} (${detail.day || ''} ${detail.time || ''}, ${detail.cohort || ''})`;
    }

    if (calloutEl && calloutTitle && calloutEmoji && calloutMeta) {
      calloutEmoji.textContent = detail.emoji || '🐾';
      calloutTitle.textContent = `${detail.emoji || ''} ${detail.animal || detail.name || 'Skupinka'} • ${detail.day || ''} ${detail.time || ''}`;
      calloutMeta.textContent = `${detail.cohort || ''} • ${detail.status === 'waiting_list' ? '⏳ Přednostní čekací listina' : '🎁 1. ukázková lekce 100% ZDARMA'}`;
      calloutEl.classList.remove('hidden');

      // Visual pulse highlight
      calloutEl.classList.add('ring-4', 'ring-amber-400', 'animate-pulse');
      setTimeout(() => {
        calloutEl.classList.remove('animate-pulse');
      }, 2500);

      // Smooth scroll directly to the form callout
      calloutEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }

    // Automatically set corresponding day in preferred days selector
    if (detail.day && ['Pondělí', 'Úterý', 'Středa', 'Čtvrtek', 'Pátek'].includes(detail.day)) {
      selectedDays.clear();
      selectedDays.add(detail.day);
      updateDaysState();
    }

    // Automatically synchronize corresponding grade level in Level dropdown
    if (detail.cohort) {
      let targetLevel = '';
      if (detail.cohort.includes('1.–3') || detail.cohort.includes('1-3')) {
        targetLevel = 'ZŠ 1. stupeň (1.–3. třída)';
      } else if (detail.cohort.includes('4.–6') || detail.cohort.includes('4-6')) {
        targetLevel = 'ZŠ 2. stupeň (4.–6. třída)';
      } else if (detail.cohort.includes('7.–9') || detail.cohort.includes('7-9')) {
        targetLevel = 'ZŠ 2. stupeň (7.–9. třída)';
      } else if (detail.cohort.includes('13') || detail.cohort.includes('Maturita') || detail.cohort.includes('7.–13')) {
        targetLevel = 'Střední škola / Maturita';
      }

      if (targetLevel) {
        const levelSelect = document.getElementById('level') as HTMLSelectElement | null;
        if (levelSelect) {
          levelSelect.value = targetLevel;
          levelSelect.dispatchEvent(new Event('change', { bubbles: true }));
        }
      }
    }

    // Smart auto-collapse: hide redundant segment & preference pickers for a friction-free booking experience
    if (segmentDropdown) segmentDropdown.classList.add('hidden');
    if (prefFields) prefFields.classList.add('hidden');
    if (togglePrefWrapper) togglePrefWrapper.classList.remove('hidden');
    if (togglePrefIcon) togglePrefIcon.textContent = '▾';

    // Update CTA button text if waiting list
    if (btnText) {
      if (detail.status === 'waiting_list') {
        btnText.textContent = "📝 Zapsat se na čekací listinu";
      } else {
        btnText.textContent = defaultSubmitText;
      }
    }
  }

  function clearSelectedGroup() {
    if (calloutEl) calloutEl.classList.add('hidden');
    if (selectedGroupInput) selectedGroupInput.value = '';
    // Restore full form options
    if (segmentDropdown) segmentDropdown.classList.remove('hidden');
    if (prefFields) prefFields.classList.remove('hidden');
    if (togglePrefWrapper) togglePrefWrapper.classList.add('hidden');
    if (segmentSelect) applySegmentMode(segmentSelect.value);
  }

  clearGroupBtn?.addEventListener('click', clearSelectedGroup, true);

  if (togglePrefBtn && prefFields) {
    togglePrefBtn.addEventListener('click', () => {
      const isHidden = prefFields.classList.contains('hidden');
      if (isHidden) {
        prefFields.classList.remove('hidden');
        if (segmentDropdown) segmentDropdown.classList.remove('hidden');
        if (togglePrefIcon) togglePrefIcon.textContent = '▴';
      } else {
        prefFields.classList.add('hidden');
        if (segmentDropdown) segmentDropdown.classList.add('hidden');
        if (togglePrefIcon) togglePrefIcon.textContent = '▾';
      }
    }, true);
  }

  window.addEventListener('valek:select-group', ((e: CustomEvent) => {
    setSelectedGroup(e.detail);
  }) as EventListener);

  async function triggerCelebration() {
    try {
      const confetti = (await import('canvas-confetti')).default;
      confetti({
        particleCount: 90,
        spread: 70,
        origin: { y: 0.6 },
        colors: ['#F59E0B', '#10B981', '#FBBF24', '#2A190F']
      });
      setTimeout(() => {
        confetti({
          particleCount: 50,
          angle: 60,
          spread: 60,
          origin: { x: 0.1, y: 0.7 },
          colors: ['#F59E0B', '#10B981', '#FBBF24']
        });
        confetti({
          particleCount: 50,
          angle: 120,
          spread: 60,
          origin: { x: 0.9, y: 0.7 },
          colors: ['#F59E0B', '#10B981', '#FBBF24']
        });
      }, 250);
    } catch (e) {
      // Graceful fallback
    }
  }

  const fieldsWrapper = document.getElementById('form-interactive-fields');
  const receiptEl = document.getElementById('booking-confirmation-receipt');
  const resetFormBtn = document.getElementById('reset-form-btn');

  const confirmParentName = document.getElementById('confirm-parent-name');
  const confirmChildName = document.getElementById('confirm-child-name');
  const confirmPhoneNum = document.getElementById('confirm-phone-num');
  const confirmProgramLabel = document.getElementById('confirm-program-label');
  const confirmGroupLabel = document.getElementById('confirm-group-label');
  const confirmGroupRow = document.getElementById('confirm-group-row');
  const confirmDaysLabel = document.getElementById('confirm-days-label');
  const addGoogleCalBtn = document.getElementById('add-google-cal-btn') as HTMLAnchorElement | null;
  const confirmWhatsAppBtn = document.getElementById('confirm-whatsapp-btn') as HTMLAnchorElement | null;

  resetFormBtn?.addEventListener('click', () => {
    form?.reset();
    clearSelectedGroup();
    selectedDays.clear();
    selectedDays.add('Kdykoliv');
    updateDaysState();
    if (phoneFeedback) phoneFeedback.innerHTML = '';
    if (emailFeedback) emailFeedback.innerHTML = '';
    phoneValidIcon?.classList.add('hidden');
    emailValidIcon?.classList.add('hidden');
    phoneInput?.classList.remove('border-emerald-500', 'bg-emerald-50/20', 'border-amber-400');
    phoneInput?.classList.add('border-parchment-300');
    receiptEl?.classList.add('hidden');
    fieldsWrapper?.classList.remove('hidden');
    fieldsWrapper?.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }, true);

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (!form || !submitBtn) return;

    // Check phone valid digits length (must have at least 9 local digits)
    if (phoneInput) {
      const digits = phoneInput.value.replace(/\D/g, '').replace(/^(420|421)/, '');
      if (digits.length < 9) {
        phoneInput.focus();
        if (phoneFeedback) {
          phoneFeedback.innerHTML = '<span class="text-rose-600 font-bold">⚠️ Zadejte prosím celé 9místné telefonní číslo</span>';
        }
        submitBtn.classList.add('animate-shake');
        setTimeout(() => submitBtn.classList.remove('animate-shake'), 600);
        return;
      }
    }

    submitBtn.disabled = true;
    if (btnText) btnText.textContent = "Odesílám rezervaci...";
    successBox?.classList.add('hidden');
    errorBox?.classList.add('hidden');

    const nameVal = (document.getElementById('name') as HTMLInputElement)?.value?.trim() || "";
    const childNameVal = (document.getElementById('child-name') as HTMLInputElement)?.value?.trim() || "";
    const phoneVal = phoneInput?.value?.trim() || "";
    const emailVal = emailInput?.value?.trim() || "";
    const segmentVal = segmentSelect?.value || "";
    const levelVal = (document.getElementById('level') as HTMLSelectElement)?.value || "";
    const daysVal = (document.getElementById('preferred-days-input') as HTMLInputElement | null)?.value || "Kdykoliv v týdnu";
    const selectedGroupVal = selectedGroupInput?.value || "";
    const messageVal = (document.getElementById('message') as HTMLTextAreaElement)?.value?.trim() || "";

    const formData = new FormData(form);
    const accessKey = form.dataset.accessKey || "";

    // Honeypot spam check
    const isBot = (form.querySelector('input[name="botcheck"]') as HTMLInputElement)?.checked;
    if (isBot) {
      handleSuccess(nameVal, childNameVal, phoneVal, segmentVal, selectedGroupVal, daysVal);
      return;
    }

    const lowerSeg = segmentVal.toLowerCase();
    const isAdult = lowerSeg.includes('dospěl') || lowerSeg.includes('konverzace');
    const isTeen = lowerSeg.includes('středoškoláci') || lowerSeg.includes('maturita') || lowerSeg.includes('střední');

    // Add clean, human-readable labels for Mr. Válek's Web3Forms email
    if (isAdult) {
      formData.append("1_Cele_jmeno", nameVal);
      formData.append("2_Dite_a_vek", "N/A (Dospělý zájemce)");
    } else if (isTeen) {
      formData.append("1_Jmeno_studenta", nameVal);
      if (childNameVal) formData.append("2_Jmeno_rodice", childNameVal);
    } else {
      formData.append("1_Jmeno_rodice", nameVal);
      if (childNameVal) formData.append("2_Dite_a_vek", childNameVal);
    }
    formData.append("3_Telefon", phoneVal);
    formData.append("4_Email", emailVal);
    formData.append("5_Vybrany_program", segmentVal);
    if (selectedGroupVal) formData.append("6_Zvireci_skupinka", selectedGroupVal);
    formData.append("7_Preferovane_dny", daysVal);
    if (levelVal) formData.append("8_Uroven_trida", levelVal);
    const contactLoc = form.dataset.contactLocation || "Růžová 1238, Uherské Hradiště (100% naživo naproti ZŠ UNESCO)";
    formData.append("9_Misto_vyuky", contactLoc);
    if (messageVal) formData.append("10_Poznamka", messageVal);

    // Remove internal/raw field keys so Web3Forms only sends the clean numbered Czech fields to Mr. Válek's inbox
    const rawKeys = ['name', 'child_name', 'phone', 'email', 'segment', 'level', 'days', 'preferred_days', 'selected_group', 'message', 'botcheck', 'forma'];
    rawKeys.forEach(k => formData.delete(k));

    // 1. Call internal server endpoint for autoresponder / receipts (primary reliable endpoint)
    let internalSuccess = false;
    try {
      const serverRes = await fetch('/api/send-reservation', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          name: nameVal,
          childName: isAdult ? "N/A (Dospělý)" : childNameVal,
          phone: phoneVal,
          email: emailVal,
          segment: segmentVal,
          selectedGroup: selectedGroupVal,
          days: daysVal,
          level: levelVal,
          message: messageVal
        })
      });
      if (serverRes.ok) {
        const serverData = await serverRes.json();
        if (serverData.success) {
          internalSuccess = true;
        }
      }
    } catch (serverErr) {
      console.warn('Internal reservation endpoint error:', serverErr);
    }

    // 2. Call Web3Forms for external email notification
    let web3Success = false;
    if (accessKey) {
      try {
        formData.append("access_key", accessKey);
        const emailSubject = selectedGroupVal
          ? `Nová rezervace: ${selectedGroupVal} – ${nameVal}`
          : (nameVal ? `Nová poptávka: ${segmentVal} – ${nameVal}` : "Nová poptávka z webu VALEK ACADEMY");
        formData.append("subject", emailSubject);
        formData.append("from_name", "VALEK ACADEMY Rezervace");
        
        const response = await fetch("https://api.web3forms.com/submit", {
          method: "POST",
          body: formData,
        });

        const data = await response.json();
        if (data.success) {
          web3Success = true;
        } else {
          console.warn("Web3Forms response not successful:", data);
        }
      } catch (web3Err) {
        // Typical when visitor uses uBlock Origin, Brave Shields, or AdGuard
        console.warn('Web3Forms request blocked or network error:', web3Err);
      }
    }

    // If either internal endpoint succeeded, Web3Forms succeeded, or in dev mode without key:
    if (internalSuccess || web3Success || !accessKey) {
      handleSuccess(nameVal, childNameVal, phoneVal, segmentVal, selectedGroupVal, daysVal);
    } else {
      errorBox?.classList.remove('hidden');
    }

    submitBtn.disabled = false;
    if (segmentSelect) applySegmentMode(segmentSelect.value);
  });

  function handleSuccess(name: string, childName: string, phone: string, segment: string, group: string, days: string) {
    if (confirmParentName) confirmParentName.textContent = name || "vážený zákazníku";

    const lowerSeg = (segment || '').toLowerCase();
    const isAdult = lowerSeg.includes('dospěl') || lowerSeg.includes('konverzace');
    const isTeen = lowerSeg.includes('středoškoláci') || lowerSeg.includes('maturita') || lowerSeg.includes('střední');

    if (confirmChildName) {
      if (isAdult) {
        confirmChildName.textContent = "vás osobně";
      } else if (isTeen) {
        confirmChildName.textContent = childName ? `${childName}` : "vás (studenta SŠ)";
      } else {
        confirmChildName.textContent = childName || "vaše dítě";
      }
    }

    if (confirmPhoneNum) confirmPhoneNum.textContent = phone;
    if (confirmProgramLabel) confirmProgramLabel.textContent = segment || "Výuka hrou";
    
    if (group && confirmGroupLabel && confirmGroupRow) {
      confirmGroupLabel.textContent = group;
      confirmGroupRow.classList.remove('hidden');
    } else if (confirmGroupRow) {
      confirmGroupRow.classList.add('hidden');
    }

    if (confirmDaysLabel) confirmDaysLabel.textContent = days || "Kdykoliv v týdnu";

    // Setup Google Calendar Link
    if (addGoogleCalBtn) {
      const attendee = isAdult ? 'dospělého' : (childName || 'dítě');
      const groupNote = group ? ` (skupinka: ${group})` : '';
      const calTitleTpl = form?.dataset.calTitle || "1. ukázková lekce angličtiny ZDARMA | VALEK ACADEMY";
      const calDescTpl = form?.dataset.calDesc || "Ukázková lekce angličtiny hrou v malé skupince pro {attendee}{group}.\nLektor: Josef Válek (+420 792 372 642)\nWeb: https://valekacademy.cz\nS sebou: Pouze přezůvky a dobrou náladu!";
      const calLoc = form?.dataset.receiptLocation || "Růžová 1238, 686 01 Uherské Hradiště (naproti ZŠ UNESCO)";

      const finalCalTitle = group ? `1. lekce zdarma: ${group} | VALEK ACADEMY` : calTitleTpl;
      const finalCalDesc = calDescTpl.replace('{attendee}', attendee).replace('{group}', groupNote);

      addGoogleCalBtn.href = `https://calendar.google.com/calendar/render?action=TEMPLATE&text=${encodeURIComponent(finalCalTitle)}&details=${encodeURIComponent(finalCalDesc)}&location=${encodeURIComponent(calLoc)}`;
    }

    // Setup WhatsApp Button Link with personalized message from CMS
    if (confirmWhatsAppBtn) {
      const attendee = isAdult ? 'mě' : (childName ? `mého syna/dceru ${childName}` : 'mé dítě');
      const groupNote = group ? ` (skupinka ${group})` : '';
      const waTpl = form?.dataset.whatsappTemplate || "Dobrý den, pane Válku, právě jsem odeslal/a rezervaci lekce pro {attendee}{group}. Rád/a bych se domluvil/a na konkrétním termínu.";
      const finalWaMsg = waTpl.replace('{attendee}', attendee).replace('{group}', groupNote);
      const tutorPhone = form?.dataset.contactPhone || '420792372642';

      confirmWhatsAppBtn.href = `https://wa.me/${tutorPhone}?text=${encodeURIComponent(finalWaMsg)}`;
    }

    // Hide input fields, reveal rich confirmation ticket
    if (fieldsWrapper && receiptEl) {
      fieldsWrapper.classList.add('hidden');
      receiptEl.classList.remove('hidden');
      receiptEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
    }

    triggerCelebration();
  }
</script>

<style>
  .day-pill {
    touch-action: manipulation;
    -webkit-tap-highlight-color: transparent;
    user-select: none;
    -webkit-user-select: none;
    cursor: pointer;
    transition: background-color 0.15s ease, border-color 0.15s ease, transform 0.1s ease;
  }

  /* Explicit active pill styling with priority */
  .day-pill.active {
    background-color: #fbbf24 !important; /* amber-400 */
    border-color: #f59e0b !important;     /* amber-500 */
    color: #180e08 !important;             /* espresso-950 */
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1) !important;
  }

  .day-pill.active .day-subtext {
    color: #3d2517 !important;             /* espresso-900 */
    font-weight: 700 !important;
  }

  /* Unselected pill styling */
  .day-pill:not(.active) {
    background-color: #fdfbf7;             /* parchment-50 */
    border-color: #e8dcbf;                 /* parchment-300 */
    color: #180e08;
  }

  .day-pill:not(.active) .day-subtext {
    color: #78716c;                        /* stone-500 */
    font-weight: 400;
  }

  /* Prevent persistent focus outlines on touch/mouse tap */
  .day-pill:focus:not(:focus-visible) {
    outline: none;
  }

  /* Only apply hover effects on fine pointers (mouse) to prevent sticky hover on touch */
  @media (hover: hover) and (pointer: fine) {
    .day-pill:not(.active):hover {
      border-color: #f59e0b;
      background-color: #fef3c7;
    }
    .day-pill.active:hover {
      background-color: #f59e0b !important;
      border-color: #d97706 !important;
    }
  }

  .day-pill:active {
    transform: scale(0.95);
  }
</style>
